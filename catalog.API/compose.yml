
services:
  database:
    container_name: catalog_db
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
  api:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - database

volumes:
  mongo_data: